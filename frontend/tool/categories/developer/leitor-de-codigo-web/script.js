const code=document.querySelector('#code'),highlight=document.querySelector('#highlight'),lines=document.querySelector('#lines'),stats=document.querySelector('#stats'),language=document.querySelector('#language'),status=document.querySelector('#status'),file=document.querySelector('#file');let lang='html';const samples={html:'<main class="card">\n  <h1>Nexauren</h1>\n  <p>Leitor de código web.</p>\n</main>',css:'.card {\n  display: grid;\n  gap: 12px;\n  padding: 20px;\n}',javascript:'const tool = "Nexauren";\nconsole.log(`Olá, ${tool}!`);'};function esc(s){return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}function paint(){const v=code.value;highlight.innerHTML=esc(v);const n=Math.max(1,v.split('\n').length);lines.textContent=Array.from({length:n},(_,i)=>i+1).join('\n');stats.textContent=`${n} linhas · ${v.length} caracteres`;highlight.scrollTop=code.scrollTop;highlight.scrollLeft=code.scrollLeft}function setLang(next){lang=next;language.textContent=next==='javascript'?'JavaScript':next.toUpperCase();code.placeholder=`Cole o seu código ${language.textContent} aqui...`;document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.lang===next));code.value='';paint();status.textContent='Pronto'}document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>setLang(b.dataset.lang)));code.addEventListener('input',paint);code.addEventListener('scroll',()=>{highlight.scrollTop=code.scrollTop;highlight.scrollLeft=code.scrollLeft;lines.scrollTop=code.scrollTop});code.addEventListener('keydown',e=>{if(e.key==='Tab'){e.preventDefault();const s=code.selectionStart;code.value=code.value.slice(0,s)+'  '+code.value.slice(code.selectionEnd);code.selectionStart=code.selectionEnd=s+2;paint()}});document.querySelector('#sample').addEventListener('click',()=>{code.value=samples[lang];paint();status.textContent='Exemplo carregado'});document.querySelector('#clear').addEventListener('click',()=>{code.value='';paint();status.textContent='Limpo'});document.querySelector('#copy').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(code.value);status.textContent='Código copiado'}catch{status.textContent='Não foi possível copiar'}});file.addEventListener('change',async()=>{const f=file.files?.[0];if(!f)return;const text=await f.text();const ext=(f.name.split('.').pop()||'').toLowerCase();lang=ext==='css'?'css':['js','mjs','cjs'].includes(ext)?'javascript':'html';document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));language.textContent=lang==='javascript'?'JavaScript':lang.toUpperCase();code.value=text;paint();status.textContent=`${f.name} aberto`});paint();