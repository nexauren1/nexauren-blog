const f=document.querySelector("#file"),text=document.querySelector("#text"),size=document.querySelector("#size"),sv=document.querySelector("#sv"),op=document.querySelector("#op"),ov=document.querySelector("#ov"),pos=document.querySelector("#pos"),angle=document.querySelector("#angle"),color=document.querySelector("#color"),stroke=document.querySelector("#stroke"),sw=document.querySelector("#sw"),swv=document.querySelector("#swv"),shadow=document.querySelector("#shadow"),tile=document.querySelector("#tile"),run=document.querySelector("#run"),dl=document.querySelector("#dl"),s=document.querySelector("#s"),source=document.querySelector("#source"),out=document.querySelector("#out");let img=null,b=null,urls=[];function clear(){urls.forEach(URL.revokeObjectURL);urls=[]}async function load(){try{if(!f.files[0])throw Error("Selecione uma imagem.");img=await NexaurenImage.loadImage(f.files[0]);const u=URL.createObjectURL(f.files[0]);urls.push(u);source.src=u;s.textContent=(img.naturalWidth||img.width)+" × "+(img.naturalHeight||img.height)+" · pronta."}catch(e){s.textContent=e.message}}function draw(ctx,w,h,scale){const label=(text.value||"NEXAUREN").slice(0,120),font=Math.max(10,Number(size.value)||48)*scale,alpha=Number(op.value)/100,ang=Number(angle.value||0)*Math.PI/180,pad=Math.max(18,w*.025);ctx.font="800 "+font+"px system-ui";ctx.textBaseline="middle";ctx.textAlign="center";ctx.globalAlpha=alpha;ctx.fillStyle=color.value;ctx.strokeStyle=stroke.value;ctx.lineWidth=Number(sw.value||0)*scale;function one(x,y){ctx.save();ctx.translate(x,y);ctx.rotate(ang);if(shadow.checked){ctx.shadowColor="rgba(0,0,0,.45)";ctx.shadowBlur=10*scale;ctx.shadowOffsetY=4*scale}if(ctx.lineWidth>0)ctx.strokeText(label,0,0);ctx.fillText(label,0,0);ctx.restore()}if(tile.checked){const step=Math.max(font*3,w*.22);for(let y=pad;y<h+step;y+=step)for(let x=pad;x<w+step;x+=step)one(x+(Math.floor(y/step)%2?step/2:0),y)}else{let x=w/2,y=h/2;if(pos.value.includes("r"))x=w-pad-ctx.measureText(label).width/2;if(pos.value.includes("l"))x=pad+ctx.measureText(label).width/2;if(pos.value.includes("t"))y=pad+font/2;if(pos.value.includes("b"))y=h-pad-font/2;one(x,y)}ctx.globalAlpha=1}async function make(){try{if(!img)await load();if(!img)throw Error("Selecione uma imagem.");const W=img.naturalWidth||img.width,H=img.naturalHeight||img.height,c=NexaurenImage.canvas(W,H),ctx=c.getContext("2d");ctx.drawImage(img,0,0,W,H);draw(ctx,W,H,1);b=await NexaurenImage.blob(c,"image/png",1);clear();const u=URL.createObjectURL(b);urls.push(u);out.src=u;dl.disabled=false;s.textContent="Marca-d'água aplicada com as definições escolhidas."}catch(e){s.textContent=e.message}}f.onchange=load;size.oninput=()=>sv.textContent=size.value;op.oninput=()=>ov.textContent=op.value+"%";sw.oninput=()=>swv.textContent=sw.value;run.onclick=make;dl.onclick=()=>b&&NexaurenImage.download(b,"nexauren-watermarked.png");