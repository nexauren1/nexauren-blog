const $=s=>document.querySelector(s),url=$("#url"),parts=$("#parts"),params=$("#params"),message=$("#message");const esc=s=>String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");
function part(name,value){return '<div class="part"><span>'+name+'</span><strong>'+esc(value||"—")+'</strong></div>'}
function analyze(){try{const raw=url.value.trim();if(!raw)throw new Error("Introduza uma URL.");const u=new URL(raw);parts.innerHTML=[part("Protocolo",u.protocol),part("Hostname",u.hostname),part("Porta",u.port||"(padrão)"),part("Origem",u.origin),part("Caminho",u.pathname),part("Fragmento",u.hash||"—"),part("URL completa",u.href)].join("");const entries=[...u.searchParams.entries()];params.innerHTML=entries.length?entries.map(([k,v])=>'<div class="param"><span>'+esc(k)+'</span><span>'+esc(v)+'</span></div>').join(""):'<div class="empty">Nenhum parâmetro de consulta.</div>';message.textContent="URL analisada com sucesso.";message.className="message ok"}catch(e){parts.innerHTML='<div class="empty">Não foi possível analisar.</div>';params.innerHTML='<div class="empty">Sem parâmetros.</div>';message.textContent=e.message;message.className="message error"}}
$("#analyze").addEventListener("click",analyze);$("#sample").addEventListener("click",()=>{url.value="https://nexaurenstory.com/tool/?category=tecnologia&search=regex#lab";analyze()});$("#clear").addEventListener("click",()=>{url.value="";parts.innerHTML='<div class="empty">Introduza uma URL e analise.</div>';params.innerHTML='<div class="empty">Nenhum parâmetro analisado.</div>';message.textContent="Aguardando uma URL.";message.className="message idle"});url.addEventListener("keydown",e=>{if(e.key==="Enter")analyze()});