const $=s=>document.querySelector(s);const base=$("#base"),freq=$("#freq"),priority=$("#priority"),lastmod=$("#lastmod"),urls=$("#urls"),code=$("#code"),count=$("#count"),message=$("#message");const esc=s=>String(s??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&apos;");
function normalizeUrl(raw){const value=raw.trim();if(!value)return null;if(/^https?:\/\//i.test(value))return value;const root=base.value.trim().replace(/\/+$/,"");if(!root)throw new Error("Informe a URL base ou use URLs absolutas.");return root+"/"+value.replace(/^\/+/,"")}
function generate(){try{const root=base.value.trim();if(root&&!/^https?:\/\//i.test(root))throw new Error("A URL base precisa começar por http:// ou https://.");const list=urls.value.split(/\r?\n/).map(normalizeUrl).filter(Boolean);const unique=[...new Set(list)];if(!unique.length)throw new Error("Adicione pelo menos uma URL.");const today=lastmod.value?lastmod.value:new Date().toISOString().slice(0,10);const lines=['<?xml version="1.0" encoding="UTF-8"?>','<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">',...unique.flatMap(u=>['  <url>','    <loc>'+esc(u)+'</loc>','    <lastmod>'+today+'</lastmod>','    <changefreq>'+freq.value+'</changefreq>','    <priority>'+priority.value+'</priority>','  </url>']),'</urlset>'];code.textContent=lines.join("\n");count.textContent=unique.length+" URL"+(unique.length===1?"":"s");message.textContent="Sitemap gerado com sucesso.";message.className="message ok"}catch(e){code.textContent="";count.textContent="0 URLs";message.textContent=e.message;message.className="message error"}}
function sample(){base.value="https://nexaurenstory.com";lastmod.value=new Date().toISOString().slice(0,10);freq.value="weekly";priority.value="0.8";urls.value="/\n/blog/\n/tool/\n/account";generate()}
$("#generate").addEventListener("click",generate);$("#sample").addEventListener("click",sample);$("#clear").addEventListener("click",()=>{base.value="";lastmod.value="";urls.value="";code.textContent="O sitemap aparecerá aqui.";count.textContent="0 URLs";message.textContent="Pronto para gerar.";message.className="message"});$("#copy").addEventListener("click",async()=>{try{await navigator.clipboard.writeText(code.textContent);message.textContent="XML copiado.";message.className="message ok"}catch{}});$("#download").addEventListener("click",()=>{if(!code.textContent.startsWith("<?xml"))return;const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([code.textContent],{type:"application/xml"}));a.download="sitemap.xml";a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)});