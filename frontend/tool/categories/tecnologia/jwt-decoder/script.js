const $=id=>document.getElementById(id);function decodePart(v){try{return JSON.stringify(JSON.parse(atob(v.replace(/-/g,'+').replace(/_/g,'/'))),null,2)}catch{return 'Não foi possível interpretar'}}$('decode').onclick=()=>{const p=$('jwt').value.split('.');if(p.length!==3){alert('JWT inválido');return}$('header').textContent=decodePart(p[0]);$('payload').textContent=decodePart(p[1]);$('signature').textContent=p[2]};