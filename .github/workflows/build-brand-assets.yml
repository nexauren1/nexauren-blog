name: Build brand assets

on:
  push:
    branches: [main]
    paths:
      - "frontend/favicon.svg"
      - "frontend/nexauren-story-favicon.svg"
      - "frontend/og-image.svg"
      - "frontend/social-preview.svg"
      - "frontend/nexauren-story-social-preview.svg"
      - ".github/workflows/build-brand-assets.yml"
  workflow_dispatch:

concurrency:
  group: build-brand-assets-main
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate PNG/JPEG assets
        run: |
          set -e
          sudo apt-get update
          sudo apt-get install -y imagemagick
          rm -f frontend/nexauren-story-favicon.png frontend/nexauren-story-favicon.ico frontend/nexauren-story-apple-touch-icon.png
          convert -background none frontend/nexauren-story-favicon.svg -resize 512x512 PNG32:frontend/nexauren-story-favicon.png
          convert -background none frontend/nexauren-story-favicon.svg -resize 180x180 PNG32:frontend/nexauren-story-apple-touch-icon.png
          convert -background none frontend/nexauren-story-favicon.svg -define icon:auto-resize=64,48,32,16 frontend/nexauren-story-favicon.ico
          cp frontend/nexauren-story-favicon.png frontend/favicon.png
          cp frontend/nexauren-story-apple-touch-icon.png frontend/apple-touch-icon.png
          convert -background none frontend/social-preview.svg -quality 100 frontend/social-preview.png
          convert -background none frontend/nexauren-story-social-preview.svg -quality 100 frontend/nexauren-story-social-preview.png
          convert -background none frontend/social-preview.svg -quality 88 frontend/og-image.jpg
      - name: Commit generated assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f frontend/favicon.png frontend/apple-touch-icon.png frontend/nexauren-story-favicon.png frontend/nexauren-story-favicon.ico frontend/nexauren-story-apple-touch-icon.png frontend/social-preview.png frontend/nexauren-story-social-preview.png frontend/og-image.jpg
          if git diff --cached --quiet; then
            echo "Brand assets are already current."
          else
            git commit -m "Generate brand preview assets"
            for attempt in 1 2 3 4 5; do
              git fetch origin main
              git rebase origin/main
              if git push origin HEAD:main; then
                echo "Brand assets pushed successfully."
                exit 0
              fi
              echo "Push race detected; retrying ($attempt/5)..."
              git reset --hard origin/main
              git add -f frontend/favicon.png frontend/apple-touch-icon.png frontend/nexauren-story-favicon.png frontend/nexauren-story-favicon.ico frontend/nexauren-story-apple-touch-icon.png frontend/social-preview.png frontend/nexauren-story-social-preview.png frontend/og-image.jpg
              git commit -m "Generate brand preview assets" || true
            done
            echo "Unable to push generated brand assets after retries."
            exit 1
          fi
